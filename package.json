{
  "name": "geekbird-checks",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "python tests/run_browser.py"
  },
  "devDependencies": { "playwright": "1.63.0" },
  "engines": { "node": ">=22" }
}
